import React from "react";

export const LeftSection = () => {
    return (
        <div className="section-subtitle jobs-subtitle">
            <hr className="section-subtitle__rule jobs-subtitle__rule"/>Current Openings
        </div>
    );
}